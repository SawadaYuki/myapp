<ul>
	<% @users.each do |user| %>
	<% if user.username == "sawadayuki" %>
	<% next %>
	<% end %>
	<li>

<%
=begin
%>
		<%= link_to user.username, user_path(user.id)%>
		(ストーリー数:<%= user.stories.count %>)

<%
=end
%>
		<%= link_to user.username,admin_user_path(user.id) %>(ストーリー数:<%= user.stories.count %>)

		<%= link_to "[Edit]",edit_admin_user_path(user.id) %>

		<%= link_to "[Delete]",admin_user_path(user.id), method: :delete,data: {confirm: "are you sure?"} %>

	</li>
	<% end %>

</ul>


